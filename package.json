{
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install && cd ../backend && npm install",
    "heroku-postbuild": "cd backend && npm run build:ui",
    "start": "cd backend && npm start"
  }
}